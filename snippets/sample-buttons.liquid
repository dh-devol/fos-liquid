{% assign get_metafield = product.metafields.custom %}
{% assign samples = get_metafield.sample.value %}

<div {{ block.shopify_attributes }}>
  Test
  {% for sample in samples %}
    {{ sample.sku }}
  {% endfor %}
</div>
